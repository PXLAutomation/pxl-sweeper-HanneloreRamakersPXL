Phase 1 complete: basic project scaffold created.
- Added `package.json` with `type: "module"` and `npm test` script.
- Added `index.html`, `css/styles.css`, and `js/game.js` with placeholder layout and app initialization.
- Verified that `npm test` runs successfully.
